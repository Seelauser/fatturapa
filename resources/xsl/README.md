# Foglio di stile AdE

To enable `StylesheetRenderer::renderHtml()`, place the official
"Foglio di stile fattura ordinaria" XSL from fatturapa.gov.it here (any
`*.xsl` filename works). Not vendored — same license policy as the XSD.
Requires the `php-xsl` extension.
